{% extends 'CpmJovenesBundle:Layout:admin_layout.html.twig' %}

{% block title %}Modificar auditorio{% endblock %}
 
{% block content %}

<form action="{{ path('auditorio_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('auditorio') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}