	<script>
   			$(document).ready(function() { 
   					$("#cpm_jovenesbundle_proyectotype_escuela_tipoInstitucion").change(function(event) { 
   						if ($(event.target).val() == "")
   							$("#cpm_jovenesbundle_proyectotype_escuela_otroTipoInstitucion").removeAttr("disabled");
   						else 
   							$("#cpm_jovenesbundle_proyectotype_escuela_otroTipoInstitucion").attr("disabled","disabled");
   					}).trigger('change');
   					
   					
   					
   			});
   		</script>

      {# incluyo el formulario de seleccion de distrito y localidad  #}  
      {% include "CpmJovenesBundle:Default:localidad_distrito_form.html.twig" %}
	      			
	{{ form_row(form.escuela.localidad)}}
	
	{{ form_row(form.escuela.tipoInstitucion)}}
	{{ form_row(form.escuela.otroTipoInstitucion)}}
	
	{{ form_row(form.escuela.tipoEscuela)}}
	
	{{ form_row(form.escuela.nombre)}}
	{{ form_row(form.escuela.numero) }}
	{{ form_row(form.escuela.email)}}
	{{ form_row(form.escuela.telefono)}}
	{{ form_row(form.escuela.domicilio)}}
	{{ form_row(form.escuela.codigoPostal)}}
	{{ form_row(form.escuela.director)}}
	   

