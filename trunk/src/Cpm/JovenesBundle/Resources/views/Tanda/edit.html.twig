{% extends 'CpmJovenesBundle:Layout:admin_layout.html.twig' %}

{% block title %}Editar Tanda{% endblock %}

{% block content %}


<form action="{{ path('tanda_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
NO DEBE EDITAR LA TANDA ASI
       <!-- <button type="submit">Editar</button>-->
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('tanda') }}">
            Volver al listado
        </a>
    </li>
</ul>
{% endblock %}
