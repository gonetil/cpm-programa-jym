{% extends 'CpmJovenesBundle:Layout:admin_layout.html.twig' %}

{% block title %}Nueva Presentacion Interna{% endblock %}

{% block content %}

<h1>Nueva PresentacionInterna</h1>

<form action="{{ path('presentacioninterna_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Crear</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('presentacion') }}">
            Cancelar
        </a>
    </li>
</ul>

{% endblock %}