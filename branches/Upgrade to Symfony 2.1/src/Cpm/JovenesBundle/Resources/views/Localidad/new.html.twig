{% extends 'CpmJovenesBundle:Layout:admin_layout.html.twig' %}

{% block title %}Agregar Localidad{% endblock %}

{% block content %}

<form action="{{ path('localidad_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Guardar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('localidad') }}">
            Volver al listado
        </a>
    </li>
</ul>

{% endblock %}