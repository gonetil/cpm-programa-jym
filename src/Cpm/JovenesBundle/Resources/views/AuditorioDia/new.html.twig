{% extends 'CpmJovenesBundle:Layout:admin_layout.html.twig' %}

{% block title %}Crear Auditorio en Día {% endblock %}
 
{% block content %}

<form action="{{ path('auditoriodia_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Crear</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('auditoriodia') }}">
           Volver al listado
        </a>
    </li>
</ul>
{% endblock %}