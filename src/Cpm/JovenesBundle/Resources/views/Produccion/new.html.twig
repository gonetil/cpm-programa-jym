{% extends 'CpmJovenesBundle:Layout:admin_layout.html.twig' %}

{% block title %}Agregar Tipo de Producción{% endblock %}

{% block content %}

<form action="{{ path('produccion_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Agregar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('produccion') }}">
            Volver al listado
        </a>
    </li>
</ul>
{% endblock %}
