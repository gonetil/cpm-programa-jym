<h1>InstanciaEvento creation</h1>

<form action="{{ path('instancia_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('instancia') }}">
            Back to the list
        </a>
    </li>
</ul>
