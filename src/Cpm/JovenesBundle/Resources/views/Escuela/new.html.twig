{% extends 'CpmJovenesBundle:Layout:admin_layout.html.twig' %}

{% block content %}

<h1>Agregar Escuela</h1>

<form action="{{ path('escuela_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Agregar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('escuela') }}">
            Volver al listado
        </a>
    </li>
</ul>
{% endblock %}